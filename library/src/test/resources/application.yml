spring:
  profiles:
    active: test
    include: jpa
  datasource:
     url: jdbc:h2:mem:library_test
     initialization-mode: always
     schema: schema/schema.sql
     data: schema/test-data.sql
  jpa:
     hibernate:
       ddl-auto: update